// If the output is bold
#define BOLD
// If the background should be rainbowed
#define BG
// If the foreground (text) should be rainbowed
#define FG
// Skip spaces (looks weird with BG enabled)
#define SKIPSPACES
// The color offset of the BG (if enabled) from the FG (radians)
#define BGOFFSET 0.3
// The step used for each character (radians)
#define XITER 0.1
// The step used for each line (radians)
#define YITER 0.1
// Length of the buffer used for reading input
#define BUFLEN 5
